<div class="text-center">
    <h1 class="display-4">Recipes</h1>
    <ul>
      @if (@Model.Count == 0)
      {
        <h2>Currently there are no recipes.</h2>
      }
      @foreach (Recipe recipe in Model)
      {
        <li><a href="recipes/@recipe.id">@recipe.name</a></li>
      }
    </ul>
    <a href="recipes/new">Add a food recipe!</a>
</div>
